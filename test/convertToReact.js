'use strict';

import should from 'should';
import convertToReact from '../lib/convertToReact';

describe('convertToReact', () => {
    it('init', () => {
        should.exist(convertToReact);
    });
});
