'use strict';

import should from 'should';
import buildClassName from '../lib/buildClassName';

describe('buildClassName', () => {
    it('init', () => {
        should.exist(buildClassName);
    });
});
