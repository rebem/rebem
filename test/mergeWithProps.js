'use strict';

import should from 'should';
import mergeWithProps from '../lib/mergeWithProps';

describe('mergeWithProps', () => {
    it('init', () => {
        should.exist(mergeWithProps);
    });
});
